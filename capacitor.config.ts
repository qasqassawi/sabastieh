import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sebastia.bakery',
  appName: 'سبسطية',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
